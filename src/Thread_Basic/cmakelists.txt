# CMakeLists.txt
cmake_minimum_required(VERSION 2.6)
 
# 项目名称
project(Learn_Thread_Basic)
 
#以下三项必须填写一项
add_executable(Learn_Thread_Basic main.cpp) 
#add_library(common STATIC util.cpp) # 生成静态库
#add_library(common SHARED util.cpp) # 生成动态库或共享库